export function pageTitle(title: string): string {
  return `${title} | PDF Toolkit`;
}

export const siteName = 'PDF Toolkit';
export const siteUrl = 'https://pdftoolkit.app';
export const defaultDescription =
  'Free, unlimited PDF tools — merge, split, rotate, compress — running 100% in your browser. No upload, no signup, no watermark.';
